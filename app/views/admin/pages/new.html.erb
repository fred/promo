<h1>New page</h1>

<%= form_for(@page, :url => { :action => "create" }, :html => { :multipart => true }) do |f| %>
  <%= render 'form', :f => f %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
