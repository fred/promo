<h1>Editing page</h1>

<%= form_for(@page, :url => { :action => "update" }, :html => { :multipart => true }) do |f| %>
  <%= render 'form', :f => f %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
