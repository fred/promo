<h1>Listing images</h1>
<br />
<% @images.each do |image| %>
    <div class="image_item">
      <%= link_to(image_tag(image.data(:medium), :alt => image.title),image, :title => image.title) %>
    </div>
<% end %>

<div class="clear"></div>